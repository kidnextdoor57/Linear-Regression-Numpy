# Linear-Regression-Numpy
Linear Regression built from scratch using only NumPy , including a custom StandardScaler, gradient descent, and MSE loss, then benchmarked against scikit-learn on a salary dataset.
